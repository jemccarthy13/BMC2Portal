/**
 * 
 * TODO -- Convert these to classes (particularly BRAA/Braaseye)
 * so they can have a .draw() function that utilizes the 
 * PaintBrush. And so things like Point can have .getBRTo(othPt)
 * 
 */

export type AltStack = {
    stack: string,
    fillIns: string,
}